import React from 'react';

export default function Guess(props) {
  const [guess, setGuess] = React.useState('false');

  const followersImgs = document.querySelector('.follower-img');

  console.log(followersImgs);
}
